
  # Interactive Report Builder

  This is a code bundle for Interactive Report Builder. The original project is available at https://www.figma.com/design/4UKmdTDVI4MKsN0CkJciDX/Interactive-Report-Builder.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  